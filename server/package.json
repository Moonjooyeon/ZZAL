{
  "name": "zzal-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "이짤이이짤 API 서버",
  "engines": { "node": ">=18" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0"
  },
  "comment": "의존성은 AI 기능에만 필요합니다. 설치하지 않아도 서버는 그대로 뜹니다.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  }
}
